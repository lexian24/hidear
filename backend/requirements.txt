# ============================================================================
# Hidear Backend Dependencies
# ============================================================================
# FastAPI and server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0

# Celery and Redis for async task processing
celery==5.3.4
redis==5.0.1

# Core ML dependencies - LOCKED VERSIONS for pyannote.audio compatibility
torch==2.2.0
torchaudio==2.2.0
numpy==1.26.4  # Last stable 1.x version, required for pyannote.audio
scipy==1.11.4

# Audio processing
librosa==0.10.1
soundfile==0.12.1
pyannote.audio==3.1.1  # Locked to tested version
speechbrain==1.0.0  # Required for inference module

# Database
sqlalchemy==2.0.23
aiosqlite==0.19.0

# Utilities
python-dotenv==1.0.0
requests==2.31.0
pydub==0.25.1
httpx==0.25.2
email-validator==2.1.0  # Required for Pydantic EmailStr validation

# Additional pyannote.audio dependencies (implicit, now explicit)
pytorch-lightning==2.1.2
torchmetrics==1.2.1
asteroid-filterbanks==0.4.0
pyannote.core==5.0.0
pyannote.database==5.0.1
pyannote.metrics==3.2.1
